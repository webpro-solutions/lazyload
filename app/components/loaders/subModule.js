/*!
 * Sub Module Test Loader
 * This is test file and can be used as an example loader.
 * */
/* eslint no-console: 0 */
export default (element, options) => {
  console.log('Sub Module Loaded', element, options);
};
